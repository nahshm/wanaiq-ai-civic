import React from 'react';
import { PositionClaimWizard } from '@/components/governance/PositionClaimWizard';

const ClaimPositionPage = () => {
    return (
        <div className="container mx-auto py-8">
            <PositionClaimWizard />
        </div>
    );
};

export default ClaimPositionPage;
